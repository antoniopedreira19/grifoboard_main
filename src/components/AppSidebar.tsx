
// This component is now replaced by CustomSidebar.tsx
import { useAuth } from "@/context/AuthContext";
import { useLocation, useNavigate } from "react-router-dom";

export function AppSidebar() {
  // This component is deprecated and only kept for compatibility reasons
  // Use CustomSidebar instead
  return null;
}

export default AppSidebar;
